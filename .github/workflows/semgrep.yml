name: Semgrep

on:
  pull_request:
    branches: ["main", "develop"]
  push:
    branches: ["main", "develop"]
  schedule:
    - cron: "37 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  semgrep:
    name: Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep

    steps:
      - name: Checkout
        uses: actions/checkout@SHA_REAL_DE_CHECKOUT # v4

      - name: Run Semgrep
        run: semgrep scan --error --config p/default --config p/security-audit --config p/secrets .
